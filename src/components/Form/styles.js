import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    backgroundColor: "#fff",
    borderRadius: 10,
    padding: 15,
    marginBottom: 15,
  },
  title: {
    fontWeight: "700",
    fontSize: 15,
  },
  discount: {
    marginTop: 10,
    fontSize: 20,
  },
});

export default styles;
