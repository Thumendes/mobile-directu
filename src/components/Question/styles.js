import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    backgroundColor: "#555",
    borderRadius: 10,
    padding: 15,
    marginBottom: 15,
  },
  title: {
    fontWeight: "700",
    fontSize: 15,
    color: "#fff",
    marginBottom: 10,
  },
  input: {
    padding: 10,
    backgroundColor: "#aaa",
  },
});

export default styles;
